{
  "name": "design_patterns",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "execute": "ts-node-dev --transpile-only --ignore-watch node_modules --respawn"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  }
}
